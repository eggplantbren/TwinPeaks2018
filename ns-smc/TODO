* Parallelise both the NS sampler and the Switch Sampler
* Include tiebreakers in both samplers
* Plot logZ/H/<f>/<g> maps
* Implement more examples
* Implement thinning
* Implement depth="auto" for NS
* Generalise to more than two scalars?
* Finish canonical.create_canonical()
* Remove dnest4 dependency in Python code
* Make a Python package
