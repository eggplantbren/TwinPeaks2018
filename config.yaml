# Configuration file for TwinPeaks2018
---
num_particles: 100
mcmc_steps: 3000            # Average number of MCMC steps per iteration
variable_mcmc_steps: true   # Should the number of MCMC steps vary?
thin: 50
num_threads: 8
depth: 500.0
rng_seed: "auto"            # Can be "auto" or a non-negative integer
switch_sampler_reps: 1000   # Number of repetitions of switch sampling
